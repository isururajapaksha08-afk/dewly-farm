import ManagementTable from "../common/ManagementTable";

export default function () {
  return (
    <ManagementTable
      title=""
      columns={["Name", "Status", "Notes"]}
      rows={[]}
    />
  );
}
